import { Slider } from './index';

const meta = {
  component: Slider,
  args: {
    name: 'Volumen',
    min: 0,
    max: 100,
    step: 1,
  },
};

export default meta;

export const Primary = {
  args: {},
};
